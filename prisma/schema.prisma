generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Hello {
  id      Int    @id @default(autoincrement())
  message String
}
